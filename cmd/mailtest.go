package main

/*
import (
	"fmt"
	"log"
	"time"

	gmailsmtp "github.com/tkobil/earnings_report/pkg"
)

func main() {
	for {
		to := []string{"ananth.ma@gmail.com"}
		result, err := gmailsmtp.SendEmailSMTP(to, "test", "test")
		if err != nil {
			log.Fatal("No Response:", err)
		}
		fmt.Println("RESULT:", result)
		time.Sleep(5 * time.Minute)
	}
}
*/
